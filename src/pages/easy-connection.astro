---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Easy Connection | Modern Solutions">
  <main class="min-h-screen bg-gradient-to-b from-slate-50 to-slate-100">
    <!-- Hero Section -->
    <section class="py-20 px-4 sm:px-6 lg:px-8">
      <div class="max-w-7xl mx-auto text-center">
        <h1 class="text-5xl md:text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-purple-600 mb-6">
          Connect With Ease
        </h1>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto mb-10">
          Seamlessly connect and collaborate with our innovative solutions designed for modern businesses.
        </p>
        <button class="bg-blue-600 hover:bg-blue-700 text-white px-8 py-4 rounded-full text-lg font-semibold transition-all">
          Get Started
        </button>
      </div>
    </section>

    <!-- Features Grid -->
    <section class="py-16 px-4 sm:px-6 lg:px-8 bg-white">
      <div class="max-w-7xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-12">Why Choose Us</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {[
            {
              title: "Fast Integration",
              description: "Quick and easy setup process that gets you connected in minutes",
              icon: "🚀"
            },
            {
              title: "Secure Connection",
              description: "Enterprise-grade security protocols to protect your data",
              icon: "🔒"
            },
            {
              title: "24/7 Support",
              description: "Round-the-clock assistance from our expert team",
              icon: "💬"
            }
          ].map(feature => (
            <div class="p-6 bg-slate-50 rounded-xl hover:shadow-lg transition-all">
              <div class="text-4xl mb-4">{feature.icon}</div>
              <h3 class="text-xl font-semibold mb-2">{feature.title}</h3>
              <p class="text-gray-600">{feature.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="py-20 px-4 sm:px-6 lg:px-8 bg-gradient-to-r from-blue-600 to-purple-600">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold text-white mb-6">
          Ready to Get Connected?
        </h2>
        <p class="text-xl text-white/90 mb-8">
          Join thousands of satisfied customers who trust our connection solutions.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <button class="bg-white text-blue-600 px-8 py-4 rounded-full text-lg font-semibold hover:bg-gray-100 transition-all">
            Start Free Trial
          </button>
          <button class="bg-transparent border-2 border-white text-white px-8 py-4 rounded-full text-lg font-semibold hover:bg-white/10 transition-all">
            Contact Sales
          </button>
        </div>
      </div>
    </section>

    <!-- Testimonial Section -->
    <section class="py-16 px-4 sm:px-6 lg:px-8 bg-white">
      <div class="max-w-6xl mx-auto">
        <div class="bg-slate-50 p-8 rounded-2xl">
          <blockquote class="text-xl text-gray-600 text-center italic mb-6">
            "The easiest and most reliable connection solution we've ever used. It's transformed how we work."
          </blockquote>
          <div class="flex items-center justify-center">
            <div class="w-12 h-12 bg-gray-300 rounded-full mr-4"></div>
            <div>
              <p class="font-semibold">Sarah Johnson</p>
              <p class="text-gray-500">CTO, Tech Solutions Inc.</p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>
